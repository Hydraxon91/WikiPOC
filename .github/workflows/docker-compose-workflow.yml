name: Build and Push Images

on:
  push:
    branches: [main]

jobs:
  tests:
    uses: ./.github/workflows/backend-tests.yml
    secrets: inherit

  build-and-push:
    needs: tests
    runs-on: ubuntu-24.04
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true
    env:
      DOCKER_BUILDKIT: 1

    steps:
      - uses: actions/checkout@v5

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push backend (includes frontend SPA)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./wiki-backend/wiki-backend/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/wiki-backend:latest
          build-args: VITE_API_URL=
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Login to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Restart App Service
        run: az webapp restart --name wikipoc-backend --resource-group WikiPOC-useeast2-rg
